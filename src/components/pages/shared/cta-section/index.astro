---
const {
  tag = "SOMOS MÁS QUE UN COWORKING",
  title = "Haz crecer tu proyecto",
  description = "Tu próximo gran proyecto comienza aquí. Únete a una comunidad de profesionales que buscan lo mismo que tú: crecer, colaborar y destacar. No esperes más, el espacio ideal está a solo un clic de distancia.",
  button = "UNIRME",
  image = "https://images.pexels.com/photos/3194518/pexels-photo-3194518.jpeg",
  imageAlt = "Espacio de trabajo colaborativo con personas trabajando juntas",
} = Astro.props;
---

<section class="mx-auto p-4 sm:p-12" role="region" aria-labelledby="cta-title">
  <div
    class="relative w-full overflow-hidden h-auto sm:h-[600px] justify-center items-center flex py-32"
    role="presentation"
  >
    <div class="absolute inset-0 z-0 h-full w-full">
      <div
        class="bg-parallax"
        style={`background-image: url(${image})`}
        role="img"
        aria-label={imageAlt}
      >
      </div>
      <div
        class="absolute inset-0 bg-black/80"
        role="presentation"
        aria-hidden="true"
      >
      </div>
    </div>

    <div
      class="flex flex-col items-center justify-center text-center p-4 sm:p-6 md:p-8 relative z-10"
      role="group"
      aria-label="Sección de llamada a la acción"
    >
      <p
        class="text-stone-100 text-center text-sm sm:text-lg tracking-[0.35em] uppercase mb-4"
        aria-label={tag}
      >
        {tag}
      </p>
      <h2
        id="cta-title"
        class="text-stone-50 text-center text-3xl sm:text-5xl font-bold font-secondary uppercase"
      >
        {title}
      </h2>
      <p
        class="text-white/90 max-w-3xl text-center my-8 text-sm sm:text-base tracking-wider"
        aria-label={description}
      >
        {description}
      </p>
      <a
        href="#"
        class="rounded-full bg-transparent border border-stone-200 lg:px-12 sm:px-10 px-6 py-3 lg:py-4 text-xs sm:text-sm font-semibold text-stone-200 shadow-xs hover:bg-stone-200 hover:text-stone-950 tracking-wider transition-all focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-stone-400"
        role="button"
        aria-label={`${button} - Únete a nuestra comunidad`}
      >
        {button}
      </a>
    </div>
  </div>
</section>

<style>
  .bg-parallax {
    width: 100%;
    height: 100%;
    background-attachment: fixed;
    background-repeat: no-repeat;
    background-size: cover;
    position: absolute;
    left: 0;
    top: 0;
  }
</style>
